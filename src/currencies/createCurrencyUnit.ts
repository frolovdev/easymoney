// TODO: maybe we need custom currency or not
// THINK about it later

// import { bind } from "./../bind";
// import { CurrencyUnit } from "./types";
// function createCurrencyUnit<CU extends CurrencyUnit>(currency: CU) {
//   const instance = {} as CU;

//   instance.getCurrencyCode = bind(getCurrencyCode, instance);
//   instance.getDefaultFractionDigits = bind(getDefaultFractionDigits, instance);

//   return instance;
// }

// function getCurrencyCode() {}

// function getDefaultFractionDigits() {}
